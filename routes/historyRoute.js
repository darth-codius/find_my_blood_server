// importing the necessary modules
const router = require('express').Router();
const historyController = require('../controller/requestHistoryController');




// exporting the router
module.exports = router;